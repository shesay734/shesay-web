# 需求22-B 证据说明 (Requirement 22-B — Web「打开APP」失败回退首页)

生成时间：2026-09-14（kanban 任务 t_190b7398，承接实施 t_74a3bece、测试 t_9cb617c5、调研 t_eb09e03a）
活动仓库：C:/Users/tnagtoc/shesay-web（HEAD `c90fb8d8bc4713573ae2ed6a74cd60cda98b3763` == origin/main == 线上 GitHub Pages https://xn--1uss88i.com，t_6dd49127 已核对一致）
注：本卡工作区字段指向 AndroidStudioProjects/shesay-web，那是已判定的「非活动/过期分叉副本」（t_6dd49127 结论），活动官网目录为 C:/Users/tnagtoc/shesay-web，本证据文档写入活动仓库。

---

## 1. 范围：仅第一区「网页打开APP失败回退首页」

需求22.docx 原文（第 6-7 行，整份 docx 已由 t_eb09e03a 完整读取）：

```
用户从网页打开APP，如果没有用户下载APP，则跳转首页（首页有下载按钮）
测试报告：没有跳转（网页点击打开app open in app依然无反应）（已多次修改失败）（建议上网查询解决办法）
```

本文档只覆盖这一条（第一区）。需求22.docx 其余条目（编辑页勾选框、fab 菜单、支付弹窗、虚拟币充值等）不在本卡范围；其中「订单渠道增加虚拟币入口」（第 28 行）已有独立证据文档 `需求22-网页虚拟币充值证据.md`（t_97590178），本卡未触碰。

「成功唤起」判定边界（调研结论，见第 2 节）：**浏览器无法查询 APP 是否已安装**，只能靠「唤起后网页是否仍在可见状态」反推；因此本实现采用分层策略，已在测试中按场景覆盖，未安装/已安装的成功与失败分支验证结果见第 4 节。

---

## 2. 浏览器/系统能力边界（官方来源 + URL）

摘录自调研文档 `平台深链回退限制调研.md`（t_eb09e03a 产出，位于 C:/Users/tnagtoc/AndroidStudioProjects/shesay-web/，本仓库目录为活动副本，调研文档内容原文引用如下）。

### 2.1 Android —— 官方唯一优雅回退：intent:// + S.browser_fallback_url
- 官方文档（Chrome/Android intents 语法、browser_fallback_url 语义、BROWSABLE 要求）：
  https://developer.chrome.com/docs/android/intents
  - 官方原文（browser_fallback_url）："When an intent isn't resolved or an external application doesn't launch, the user can be redirected to the fallback URL."
  - 只有带 `android.intent.category.BROWSABLE` 的 Activity 才能被唤起；browser_fallback_url 会被 Chrome 剥离，目标 APP 看不到。
- Android 官方 Intent 参考：https://developer.android.com/reference/android/content/Intent
- Android App Links（跨平台深链正统方案，需 assetlinks.json 域名校验）：https://developer.android.com/training/app-links
- 社区佐证（需实机验证）：fallback_url 自 Chrome 40 引入——https://paul.kinlan.me/launch-app-from-web-with-fallback/ ；无 fallback 时未安装会在 Chrome 报 "Navigation is blocked"/ERR_UNKNOWN_URL_SCHEME（正是需求 22 中「无反应」的成因）——https://issues.chromium.org/issues/40894865

**边界**：未安装时由 Chrome 自己解析 fallback_url 跳回官网首页，**不需要 JS 计时**；JS 定时器方案在 Android 反而导致 "Navigation is blocked"。已安装时 APP 被唤起，页面离开前台；若用户拒绝系统唤起/系统弹窗拦截，可能伪装成「未唤起」。

### 2.2 iOS Safari —— 自定义 scheme 没有官方回退
- Apple 官方推荐 Universal Links（未安装时链接本身就是官网 URL，天然满足回退）：https://developer.apple.com/library/archive/documentation/General/Conceptual/AppSearch/UniversalLinks.html（官方原文 "If your app isn't installed, tapping a link to your website opens your website in Safari."）
- 配置要求（AASA + associated domains + HTTPS + APP 端 NSUserActivity）：https://developer.apple.com/documentation/xcode/supporting-universal-links-in-your-app
- 官方未提供任何「网页查询 scheme 是否注册」的 API（无 canOpenURL 等价物）。

**边界（无法可靠检测，必须明示）**：
- visibilitychange/document.hidden 只能说明「页面不在前台」，不能区分「APP 被唤起」「用户切别的 App」「系统弹窗」「浏览器切后台」——任何隐藏事件都是假阳性。
- 未安装时 Safari 显示系统错误弹窗 "Safari cannot open the page because the address is invalid"，页面仍可见且无 JS 回调 → 只能靠「定时器：N 秒后若仍可见则回退首页」启发式命中。
- 真实方案是 Universal Links（需 APP 侧 AASA/associated domains/NSUserActivity 改动，超出网页工程，属 APP 侧工作，未实施）。

### 2.3 Desktop —— 无官方检测，定时器+可见性回退是常用兜底
- Chrome/Edge 桌面对自定义协议弹「外部协议对话框」（External Protocol Dialog），策略位说明：https://support.google.com/chrome/a/answer/2657289 ；Edge 策略：https://learn.microsoft.com/en-us/deployedge/microsoft-edge-policies
- 桌面网页无 canOpenURL 等价 API，协议处理程序注册属系统/浏览器级。

**边界**：点击后页面通常保持可见（对话框非全屏隐藏页面），定时器回退稳定，但无法区分「用户正看对话框未选择」与「确实未安装」，可能在用户未点对话框时跳走——已知局限，需实机确认接受度。

### 2.4 平台分支选型结论（沿用调研结论）
| 平台 | 采用方案 | 回退依据 |
|---|---|---|
| Android | intent:// + S.browser_fallback_url(官网首页) | 官方机制，未安装自动跳首页，无错误弹窗 |
| iOS | 自定义 scheme + 1500ms 定时器，**仅页面仍可见**才 location.assign 首页 | 启发式（无官方回退），已接 Universal Links 为真解、未实施 |
| Desktop | 同上（自定义 scheme + 1500ms 可见性回退） | 无官方检测，定时器为常用兜底 |

---

## 3. 改动文件与文件:行号（当前工作树实际行号，git diff HEAD 已核对）

修改仅为 3 个分享页的**已有入口逻辑**（未新增 UI/按钮/页面）：

### post.html（`button#openInApp` "Open in app"）
- 新增 `launchApp()` 辅助函数 + `APP_HOME` 常量 + `openAppPending` 守卫：**33-60 行**
  - post.html:33 `const APP_HOME = 'https://xn--1uss88i.com/';`
  - post.html:34 `let openAppPending = false;`
  - post.html:35-60 `function launchApp(deepLink) { ... }`
- 入口绑定（渲染后 onclick）：**110 行**
  - post.html:110 `document.getElementById('openInApp').onclick = () => launchApp(`shesay://post?id=${encodeURIComponent(row.id)}`);`
- 行为（launchApp 内部）：
  - Android（post.html:38-44）：`deepLink.replace(/^shesay:\/\//,'intent://') + '#Intent;scheme=shesay;action=android.intent.action.VIEW;category=android.intent.category.BROWSABLE;S.browser_fallback_url=' + encodeURIComponent(APP_HOME) + ';end'` → `location.href` 赋值 intent URI；仅一个 250ms 守卫复位定时器，**无 JS 回退定时器**。
  - iOS/Desktop（post.html:45-59）：注册 `visibilitychange`+`pagehide` 监听；1500ms 定时器到期**仅当 `document.visibilityState === 'visible'`** 才 `location.assign(APP_HOME)`；`document.hidden` 或 `pagehide` → cancel()（清定时器、移除监听、复位守卫、不跳转）。
- 改动前行为（git diff 基线）：post.html 旧 onclick 直接 `location.href = shesay://post?id=`，1200ms 后若 `visibilityState==='visible' && Date.now()-started<1800` 则跳 `index.html`——无 Android intent、无成功唤起取消、无重复点击守卫。

### publication.html（`a.button.open-app` "Open in App"，原为裸锚点无回退）
- 新增 `launchApp()` + `APP_HOME` + 守卫：**30-57 行**（publication.html:30-57，同 post 结构）
- 入口绑定（渲染后给锚点挂 onclick）：**68-69 行**
  - publication.html:68 `const appLinkEl = el.querySelector('.open-app');`
  - publication.html:69 `appLinkEl.onclick = event => { event.preventDefault(); launchApp(appLinkEl.href); };`
- 深链构造：publication.html:66 `const appLink = `shesay://publication?id=${encodeURIComponent(row.id)}`;`
- 行为：与 post.html 的 launchApp 完全同构（Android intent:// / 非 Android 1500ms 可见性回退 + 取消）。
- 改动前行为：裸锚点 href=shesay://…，浏览器默认导航，**无任何回退**。

### tea-room.html（`a#openApp` "打开APP加入茶室"，原为无回退）
- 新增 `launchApp()` + `APP_HOME` + 守卫：**50-77 行**
  - tea-room.html:50 `const APP_HOME = 'https://xn--1uss88i.com/';`
  - tea-room.html:51 `let openAppPending = false;`
  - tea-room.html:52-77 `function launchApp(deepLink) { ... }`
- 深链：tea-room.html:90 `openApp.href = 'shesay://tea-room?id=' + encodeURIComponent(id);`（仅 id 有效时设置）
- 入口绑定：**91-94 行** —— `openApp.addEventListener('click', event => { event.preventDefault(); if (openApp.href.startsWith('shesay://')) launchApp(openApp.href); });`
- 行为：同构（Android intent:// / 非 Android 1500ms 可见性回退 + 取消）；tea-room 额外只在 href 以 `shesay://` 开头时才唤起（id 无效时禁用）。
- 改动前行为：href 初始 `#`，js 赋值 shesay:// 后无任何 onclick 处理、无回退。

### 回退目标（三页共用）
- APP_HOME = `https://xn--1uss88i.com/`（官网首页 index.html）；index.html:28 有下载按钮 `<a class="download-btn" href="https://play.google.com/store/apps/details?id=com.shesay.app">Download the App</a>`，满足需求「首页（首页有下载按钮）」。

### 本次未新增/未改动的文件
- **未新增 UI、按钮、页面**：3 个分享页均只改已有入口的 JS 逻辑；未新增任何 HTML 元素。
- **未触碰他人未提交改动**：工作树另有 site.css（+1-1，`.recharge-form` 样式，属虚拟币充值任务 t_97590178）、wallet.html（+50-10，USDT 表单，同属 t_97590178）、test/ 目录（含本卡测试与既有测试）、`需求22-网页虚拟币充值证据.md`（t_97590178 产出）——本次 22-B 只增加了 test/openapp-fallback.test.cjs 与本文档，其余原样保留（git status 核对见第 4 节）。

---

## 4. 真实命令与结果（本次运行，非声称）

环境：node v24.14.1（git-bash / Windows）

### 4.1 git 核对输出
```
$ git status --porcelain  (活动仓库 C:/Users/tnagtoc/shesay-web, HEAD c90fb8d8bc4713573ae2ed6a74cd60cda98b3763)
 M post.html
 M publication.html
 M site.css
 M tea-room.html
 M wallet.html
?? test/
?? "需求22-网页虚拟币充值证据.md"

$ git diff --name-only HEAD
post.html
publication.html
site.css
tea-room.html
wallet.html

$ git diff HEAD --stat
 post.html        | 37 +++++++++++++++++++++++++++++--------
 publication.html | 30 ++++++++++++++++++++++++++++
 site.css         |  2 +-
 tea-room.html    | 32 ++++++++++++++++++++++++++++++
 wallet.html      | 60 ++++++++++++++++++++++++++++++++++++++----------
 5 files changed, 142 insertions(+), 19 deletions(-)

$ git stash list
(空 —— 无 stash)

$ git rev-parse HEAD
c90fb8d8bc4713573ae2ed6a74cd60cda98b3763
```
说明：post/publication/tea-room 三个文件的 diff 内容即为第 3 节所列（launchApp 插入 + 入口绑定替换，publication/tea-room 为纯新增）；site.css 与 wallet.html 的改动属于虚拟币充值任务 t_97590178（site.css diff 仅 +1-1 的 `.recharge-form` 规则，wallet.html +50-10 为 USDT 表单），22-B 未触碰。

### 4.2 测试运行命令与输出

**a) 本卡测试文件（40 项真实执行断言，独立运行）：**
```
$ node --test test/openapp-fallback.test.cjs
✔ S3 post [ios] visibilitychange hidden cancels the fallback
✔ S3 publication [desktop] visibilitychange hidden cancels the fallback
✔ S3 publication [desktop] pagehide cancels the fallback
✔ S3 publication [ios] visibilitychange hidden cancels the fallback
✔ S3 tea-room [desktop] visibilitychange hidden cancels the fallback
✔ S3 tea-room [desktop] pagehide cancels the fallback
✔ S3 tea-room [ios] visibilitychange hidden cancels the fallback
✔ S4 post [desktop] three rapid clicks -> single fallback timer and single navigation
✔ S4 publication [desktop] three rapid clicks -> single fallback timer and single navigation
✔ S4 tea-room [desktop] three rapid clicks -> single fallback timer and single navigation
✔ S5 iOS uses plain shesay:// custom scheme with visibility guard (no intent)
✔ S5 desktop uses plain shesay:// custom scheme with visibility guard (no intent)
✔ S5 Android intent URI equals the documented browser_fallback_url format
✔ INJECT [post] removing the fallback assign -> S2 oracle fails
✔ INJECT [post] removing cancel-on-hidden -> S3 oracle fails
✔ INJECT [post] removing hidden protection -> hidden case would redirect, oracle fails
✔ INJECT [post] removing pagehide cancel -> pagehide oracle fails
✔ INJECT [post] removing the duplicate-click guard -> S4 oracle fails
✔ INJECT [post] breaking the Android intent URI -> S1 oracle fails
✔ INJECT [tea-room] removing the fallback assign -> S2 oracle fails (page-specific)
✔ INJECT [tea-room] unbinding the visibility listener -> hidden cancel oracle fails
✔ INJECT [publication] removing the duplicate-click guard -> S4 oracle fails (page-specific)
ℹ tests 40
ℹ suites 0
ℹ pass 40
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 238.718
```
（上段为 tail-30 截取；完整清单见 test/openapp-fallback.test.cjs 末尾执行结果，S1-S5 全部通过。）

**b) 全仓库套件（含既有 wallet/语法检查）：**
```
$ node --test   (仓库根目录)
✔ INJECT [post] removing the duplicate-click guard -> S4 oracle fails
✔ INJECT [post] breaking the Android intent URI -> S1 oracle fails
✔ INJECT [tea-room] removing the fallback assign -> S2 oracle fails (page-specific)
✔ INJECT [tea-room] unbinding the visibility listener -> hidden cancel oracle fails
✔ INJECT [publication] removing the duplicate-click guard -> S4 oracle fails (page-specific)
✔ wallet page wires the virtual-coin channel to order creation
✔ submitting the virtual-coin form creates a pending order and shows its number
ℹ tests 43
ℹ suites 0
ℹ pass 43
ℹ fail 0
ℹ cancelled 0
ℹ skipped 0
ℹ todo 0
ℹ duration_ms 570.2878
```
43 项全过 = 40（22-B）+ 2（既有虚拟币）+ 1（HTML 语法检查）。

**c) HTML 语法检查（CI 等价，10 个 HTML 文件）：**
```
$ node test/check-html-syntax.cjs .
checked auth.html
checked child-safety-standards.html
checked data-deletion.html
checked index.html
checked post.html
checked privacy-policy.html
checked publication.html
checked tea-room.html
checked terms-of-service.html
checked wallet.html
OK: all 10 html files check clean
exit=0
```

**d) 测试文件语法自检：**
```
$ node --check test/openapp-fallback.test.cjs
OK
```

### 4.3 测试覆盖矩阵（断言均为「真实执行」而非字符串搜索）
测试 harness：node:test + node:vm，从仓库文件**提取并执行三页真实 inline `<script>`**，真实 addEventListener/removeEventListener 注册与 click/visibilitychange/pagehide 派发，可控 fake clock（setTimeout/clearTimeout 记录并手动推进），可配置 UA（Android/iOS/desktop），location 记录每次导航。DOM 节点与网络为轻量 stub（文件头已声明，非浏览器）。

| 场景 | 断言要点（执行语义） |
|---|---|
| S1 Android（3 页） | 点击 → 恰好 1 次导航，URI 以 intent:// 开头、含 scheme=shesay、VIEW action、BROWSABLE、S.browser_fallback_url(官网首页)、;end 结尾；**无** JS 回退定时器（仅 250ms 守卫复位定时器存在）；2s 后仍无首页导航；hidden/pagehide 也不触发重定向 |
| S2 未安装（iOS+desktop × 3 页） | 自定义 scheme 导航 1 次 → 1500ms 后 location.assign(APP_HOME)，target 即三页 APP_HOME 常量；定时器清理、守卫复位（第二次点击可再次唤起）；静态断言 index.html 含 Download CTA 且为可交互元素 |
| S3 已安装/隐藏（3 页 × visibilitychange-hidden / pagehide / iOS 变体） | visibilitychange→hidden：定时器取消、两个监听移除、2s 后无重定向、仅 scheme 导航 1 次；pagehide 同理；iOS 同理 |
| S4 重复点击（3 页） | 3 次连点 → 仅 1 次 scheme 导航、1 个监听（各 1 个）、1 个定时器、1 次首页回退，导航序列 [scheme, fallback] |
| S5 平台分支（3 页） | iOS/desktop 用纯 shesay:// 自定义 scheme + visibility 守卫、不含 intent；Android intent URI 与文档格式字符串完全相等（含 encodeURIComponent 的首页） |
| INJECT ×10（失败注入） | 对同一 harness 运行会把目标行为破坏的变异脚本（删回退/删隐藏取消/删 hidden 保护/删 pagehide/删重复守卫/断 Android intent/删 visibility 监听），断言场景 oracle 必然失败 —— 证明 40 项断言是执行语义验证而非 grep |

---

## 5. 明确声明：未发布 / 未部署 / 未 commit / 未 push / 实机未验证

1. **未 commit / 未 push / 未部署**：全部改动仅存在于活动仓库工作树（git status 见 4.1），HEAD 仍为 c90fb8d 未变。上线需另行授权（git add post.html publication.html tea-room.html test/openapp-fallback.test.cjs 需求22-B证据说明.md → push origin main → GitHub Pages 自动发布，或走现有发布流程）。
2. **移动端实机未验证**：本次无 Android/iOS 真机或模拟器访问权限，全部验证为 node:vm 执行页面真实 JS（DOM/网络 stub，已声明非浏览器）。
3. **需实机验证的点（遗留风险）**：
   - Android 真机：**Chrome intent 解析器自动回退**（Chrome 拉 fallback_url 是否确实跳首页）——依赖浏览器行为，vm 只能验证 URI 格式正确（S5 字符串相等 + S1 结构断言）。
   - iOS 真机 Safari：未安装时系统错误弹窗 "Safari cannot open the page" 出现后，1500ms 定时器是否仍触发、页面是否保持可见（调研确认这是无法由网页 JS 可靠判定的场景，启发式命中率依赖弹窗不改变 visibility）。
   - 已安装真机：唤起 APP 后返回网页，确认无错误回退（触发 failed 假阳性：用户切后台/慢速唤起/系统权限弹窗）。
   - 桌面：External Protocol Dialog 弹出期间定时器可能提前把页面跳走（调研明确：页面可见时无法区分「正在看对话框」与「未安装」，1.5s 阈值是权衡值）——
   - WebView/微信内置浏览器等其他 Android 容器对 intent:// 的支持未覆盖。
4. **接入 Universal Links 未做**：iOS 真解是 Universal Links（APP 侧 AASA+associated domains+NSUserActivity），属于 APP 侧工作，本次网页工程未实施、未承诺上线效果。

---

## 6. 本次未新增 UI/按钮/页面，未触碰其它未提交改动

- 3 个分享页的改动均为**改造已有"打开APP"入口的 JS 逻辑**（post.html button 的 onclick、publication.html 裸锚点加 onclick+preventDefault、tea-room.html 锚点加 click 监听），未新增 HTML 元素/按钮/页面。
- 与需求 22-C（SheAdmin 沟通页/支付弹窗/封禁历史）及需求 22 虚拟币充值（wallet.html/site.css）为不同文件、不同任务，本卡未改动；工作树中 site.css/wallet.html 的未提交改动及 `需求22-网页虚拟币充值证据.md`、`test/` 目录（t_97590178 既有）均原样保留（4.1 git status 与 stash 空 佐证）。
- 未读取任何密码文件/密钥；tea-room.html 中 Supabase publishable key（`sb_publishable_...`）未在本文档复现（fetch 在测试中 stub）。

---

## 7. 验收自查（QA 集成断点）

| 检查项 | 状态 | 证据位置 |
|---|---|---|
| 需求原文可追溯 | ✅ | 第 1 节（需求22.docx 第 6-7 行） |
| 浏览器/系统能力边界有官方来源 | ✅ | 第 2 节（developer.chrome.com / developer.android.com / developer.apple.com / support.google.com / learn.microsoft.com 等 URL） |
| 改动文件、行号、入口、行为可追溯 | ✅ | 第 3 节（当前工作树行号 + git diff HEAD 核对） |
| 真实命令与结果 | ✅ | 第 4 节（本次实际运行：node v24.14.1，40/43 全过，HTML 10/10 clean，node --check OK） |
| 未发布/未部署/未 commit/push、实机未验证、遗留风险 | ✅ | 第 5 节（git status、stash 空、HEAD 未变） |
| 未新增 UI/按钮/页面、未触碰他人改动 | ✅ | 第 6 节（git status + 既有文档互证） |

遗留待后续 QA 决策/执行项：
- Android 真机验证 intent 回退（浏览器行为，最可能出问题点）。
- iOS 实机验证 Safari 弹窗 + 1500ms 定时交互。
- 桌面外链对话框与 1.5s 定时器冲突的体验决策（可接受或调阈值）。
- 是否推进 iOS Universal Links（APP 侧工作，需单独排期）。