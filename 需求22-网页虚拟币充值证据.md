# 需求22-网页虚拟币充值证据 (Requirement 22 — Web Virtual-Coin Recharge)

生成时间:2026-09-14 (本次 kanban 任务 t_97590178)
活动仓库:C:/Users/tnagtoc/shesay-web (HEAD c90fb8d == origin/main == 线上 https://xn--1uss88i.com)
注:本任务工作区(AndroidStudioProjects/shesay-web)是分叉副本,实施落点在活动仓库。

## 1. 需求原文(需求22.docx)
- 行28:订单渠道增加虚拟币入口,记录所有通过虚拟币充值的订单
- 行30:系统赠金的充值订单类型改成"其他"类型
用户澄清:行28 指官网网页端充值渠道,不是 SheAdmin 管理端新增充值入口。

## 2. 结论:虚拟币(USDT)充值链路已存在于后端,网页端缺"记录订单"环节
- 后端 RPC(已部署线上,remote_schema 20260912220522 确认):
  - `create_usdt_recharge_request(requested_amount, requested_paid_amount, requested_payer_crypto_address, requested_payment_method_id)`
    → 写入 wallet_transactions(type='recharge', payment_method='usdt', status='pending', transaction_no 'U...', external_paid_amount, payer_crypto_address),返回订单号。GRANT EXECUTE TO authenticated。
  - `admin_confirm_usdt_recharge(transaction_id, approved)` → pending→completed(余额入账)/failed。管理员权限。
- 网页端 wallet.html 原有:读取 payment_methods(仅 usdt)展示为渠道按钮,但点击只提示收款信息,**不创建订单** → 需求"记录所有通过虚拟币充值的订单"未满足。

## 3. 本次修改(最小实现,复用现有充值 UI,不另造页面)
文件清单(均在活动仓库 C:/Users/tnagtoc/shesay-web):
- wallet.html:
  - recharge 区内新增内联表单 #rechargeForm(充值金额 USD / 实付金额 USDT / 付款地址 + 提交按钮),默认 hidden
  - 新增 openRechargeForm()/submitRecharge():USDT 渠道按钮点击展开表单;提交调用 create_usdt_recharge_request,成功后提示"订单号 X,请等待管理员确认后到账(处理中)"
  - 校验:金额≥1、实付≥充值金额、地址≥12 位,与后端 RPC 校验一致
  - USDT 按钮 onclick 从"显示地址"改为"展开表单"(不再把收款地址直接暴露在页面上)
- site.css:新增 .recharge-form 样式(间距/边框,复用现有设计变量)
- test/requirement22-virtualcoin.test.cjs:回归测试(先红后绿)
- test/check-html-syntax.cjs:CI 等价语法检查(对齐 .github/workflows/web-check.yml)
未新增页面、未新增管理端入口、未改主APP、未改后端 SQL/Edge Function(链路已存在)。

## 4. 测试命令与结果(真实运行,非字符串搜索)
- 回归测试(先写失败,后实现转绿):
  `node --test test/requirement22-virtualcoin.test.cjs` → 2 pass, 0 fail
- 全站 HTML/JS 语法检查(CI 等价):
  `node test/check-html-syntax.cjs .` → 10 html files check clean
- SQL/Edge Function 定向测试(dockerized postgres 最小骨架 + 生产 RPC 原样定义):
  场景 1-6 全部通过:
  - 认证用户下单 → 生成 U 开头订单号,pending 记录(amount/paid/address 正确)
  - 输入校验:金额<1 / 实付<金额 / 地址<12 → 23514;未知支付方式 → 23503;无会话 → 42501
  - 非管理员确认被拒(42501),订单保持 pending
  - 管理员拒绝 → failed,余额不变
  - 管理员批准 → completed,余额按充值金额入账,实付金额保留在订单上
  (测试文件临时位于 %LOCALAPPDATA%/Temp/req22_usdt_test.sql,未入库)

## 5. 远程未部署项目 / 影响
- 本任务仅本地修改,**未 commit / 未 push / 未远程部署**。上线需:git add wallet.html site.css test/ 并 push origin main(GitHub Pages 自动发布,或走现有流程)。
- 后端 RPC 已部署,无需新迁移。若线上 payment_methods 无 id 列或 RPC 缺失(以 remote_schema 为准均已存在),需先核对再上线。
- 工作区副本(AndroidStudioProjects/shesay-web)未同步本次改动(该副本有他人未提交工作,禁止覆盖)。

## 6. 行30(系统赠金类型→"其他")分开核对结果(不在本次网页端实施)
- 现状:type='system_gift' 已存在(迁移 20260911100000 写入),主APP wallet_page.dart:175 映射 '系统赠金'。
- 影响面:数据库 type 枚举、admin_adjust_user_wallet 写入值、历史数据 UPDATE 回填、主APP/SheAdmin label 映射。
- 属主APP/SheAdmin/数据库侧,与本任务(网页端充值渠道)分离;如需实施应新建本地迁移并评估远程部署影响,禁止直接远程 repair。

## 7. 与并行任务共存
活动仓库工作树同时存在他人未提交改动(post.html/publication.html/tea-room.html 的"打开APP"深链逻辑、open-app.js 等),与本次改动不同文件,原样保留未覆盖。
